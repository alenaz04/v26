<Page x:Class="finish.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Окно администратора">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0"/>
                <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"/>

                <TextBlock Text="Пароль:" Grid.Row="1" Grid.Column="0"/>
                <TextBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

                <TextBlock Text="Роль:" Grid.Row="2" Grid.Column="0"/>
                <ComboBox x:Name="cmbRole" Grid.Row="2" Grid.Column="1">
                    <ComboBoxItem Content="Администратор"/>
                    <ComboBoxItem Content="Пользователь"/>
                </ComboBox>
            </Grid>
        </Border>

        <DataGrid x:Name="dataGridUsers" Grid.Row="1" AutoGenerateColumns="True" 
                  SelectionChanged="dataGridUsers_SelectionChanged" Margin="5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnAdd" Content="Добавить" Click="btnAdd_Click" Width="80" Margin="5"/>
            <Button x:Name="btnEdit" Content="Изменить" Click="btnEdit_Click" Width="80" Margin="5"/>
            <Button x:Name="btnDelete" Content="Удалить" Click="btnDelete_Click" Width="80" Margin="5"/>
            <Button x:Name="btnUnblock" Content="Снять блокировку" Click="btnUnblock_Click" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>